# 开发环境 Dockerfile（使用本地 node_modules 版本）
# 使用 Node.js 官方镜像
FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 直接复制所有文件（包括 node_modules，如果存在）
COPY . .

# 如果 node_modules 不存在，则安装依赖
RUN if [ ! -d "node_modules" ]; then npm install --prefer-offline --no-audit; fi

# 暴露 Vite 开发服务器端口
EXPOSE 3000

# 启动开发服务器
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]

